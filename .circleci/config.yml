version: 2.1
orbs:
jobs:
  create_infra:
      docker:
        - image: amazon/aws-cli
      steps: 
        - checkout
        - run:
            name: Create CloudFormation Stack
            command: |
              aws cloudformation deploy \
                --template-file template.yml \
                --stack-name myStack-${CIRCLE_WORKFLOW_ID:0:5} \
                --region us-east-1   
# Sequential workflowww
workflows:
  myWorkflow:
    jobs:
      - create_infra